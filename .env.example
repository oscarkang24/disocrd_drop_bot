# Copy this file to .env and fill in your bot token.
DISCORD_TOKEN=your-bot-token-here

# Optional: restrict slash-command sync to one guild for instant updates while developing.
# Leave empty to sync globally (can take up to an hour to propagate).
DEV_GUILD_ID=

# Optional: path to the SQLite database file.
DATABASE_PATH=dropbot.db
